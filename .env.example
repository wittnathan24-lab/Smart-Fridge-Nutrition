USDA_API_KEY=
# Generate a random value of at least 32 characters for persistent sessions.
JWT_SECRET=
DATABASE_PATH=smart_fridge.db
# Supabase: Settings > API. Never expose these values in the browser.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Server only: account administration. Never include in browser code or Git.
SUPABASE_SECRET_KEY=
# Provision once with python -m scripts.setup_demo_account
DEMO_EMAIL=demo@smart-fridge.example
DEMO_PASSWORD=
DEMO_USER_ID=
